defmodule AuthService.Settings do
  def deliver_user_update_email_instructions(
    applied_user,
    email,
    url
  ) do

  end

  def change_user_email(user) do

  end

  def apply_user_email(user, password, user_params) do

  end

  def get_user_by_email(email) do

  end
end
